name: CI/CD

on:
  push:
    branches: master
  workflow_dispatch:

jobs:
  empaquetado:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Script para empaquetar
        run: |
          conda activate splunk-app-packager
          py C:\Users\lpuentes\Documents\actions-runner\_work\packager_script\splunk_app_packager.py demo-app
          conda deactivate
        
#  ${{ github.repository }}
  instalacion:
    needs: empaquetado
    runs-on: self-hosted
#     environment: produccion
    steps:
      - name: Copiar paquete a destino
        run: dir
        
#scp -i C:\Users\lpuentes\Downloads\llave.pem 
